import React, { memo} from 'react';
import Background from '../components/Background';

const GestionDesTaches = ({ navigation }) => {
  return(
  <Background>
    <Header>Ajouter une Tache</Header>
    
    </Background>
  )
    }
